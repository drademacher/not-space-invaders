name: Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: drademacher/not-space-invaders/.github/workflows/setup.yml@main

    - run: bun run test